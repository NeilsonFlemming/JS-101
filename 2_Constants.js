const interestRate = 0.3;
interestRate = 1;
console.log(interestRate);

//error will occur because you cant re-assing constants
//if the value of a variable is static use const
//if the value of variable will change use let